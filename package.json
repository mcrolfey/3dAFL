{
  "name": "3dafl",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "build:shared": "npm run build -w shared",
    "dev": "npm run build:shared && concurrently -k -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "headless": "npm run build:shared && npm run headless -w server",
    "build": "npm run build:shared && npm run build -w server && npm run build -w client"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
